/**
 * ExecuteGetTierCriteria - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: tiering
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteGetTierCriteria Use Case
 *
 * DDD: Application use case for get TierCriterium.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { GetTierCriteriaInput, GetTierCriteriaOutput } from "../dto/tier-criterium.dto";
import type { ExecutionContextService, IdGeneratorService } from "@assayline/services/_shared/index.js";
import type { TierCriteriumRepository } from "../ports";
import { NotFoundError } from "../errors";

export class ExecuteGetTierCriteria {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly tierCriterium: TierCriteriumRepository
  ) {}

  async execute(input: GetTierCriteriaInput): Promise<GetTierCriteriaOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.tirId();

    // All field names extracted from OpenAPI spec: none
    // Load entity via repository (no ID field - uses input directly, pass correlation ID for response meta)
    const entity = await this.tierCriterium.getTierCriteria({ ...input, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found`);
    }

    return entity as GetTierCriteriaOutput;
  }
}
