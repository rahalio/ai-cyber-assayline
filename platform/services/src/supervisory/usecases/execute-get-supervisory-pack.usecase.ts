/**
 * ExecuteGetSupervisoryPack - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: supervisory
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteGetSupervisoryPack Use Case
 *
 * DDD: Application use case for get SupervisoryPack.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { GetSupervisoryPackInput, GetSupervisoryPackOutput } from "../dto/supervisory-pack.dto";
import type { ExecutionContextService, IdGeneratorService } from "@assayline/services/_shared/index.js";
import type { SupervisoryPackRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteGetSupervisoryPack {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly supervisoryPack: SupervisoryPackRepository
  ) {}

  async execute(input: GetSupervisoryPackInput): Promise<GetSupervisoryPackOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.spvId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).packId) {
      throw new ValidationError("packId is required");
    }
    const packId = (input as any).packId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.supervisoryPack.getSupervisoryPack({ packId: packId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${packId}`);
    }

    return entity as GetSupervisoryPackOutput;
  }
}
