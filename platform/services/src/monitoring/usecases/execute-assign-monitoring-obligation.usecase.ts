/**
 * ExecuteAssignMonitoringObligation - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: monitoring
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteAssignMonitoringObligation Use Case
 *
 * DDD: Application use case for get Obligation.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { AssignMonitoringObligationInput, AssignMonitoringObligationOutput } from "../dto/obligation.dto";
import type { ExecutionContextService, IdGeneratorService } from "@assayline/services/_shared/index.js";
import type { ObligationRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteAssignMonitoringObligation {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly obligation: ObligationRepository
  ) {}

  async execute(input: AssignMonitoringObligationInput): Promise<AssignMonitoringObligationOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.monId();

    // All field names extracted from OpenAPI spec: developerTeamId, metric, modelId, ownerId, revalidationOnBreach, threshold
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).developerTeamId) {
      throw new ValidationError("developerTeamId is required");
    }
    const developerteamId = (input as any).developerTeamId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.obligation.assignMonitoringObligation({ developerTeamId: developerteamId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${developerteamId}`);
    }

    return entity as AssignMonitoringObligationOutput;
  }
}
