/**
 * ListConservatismOverlays handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: conservatism
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { listConservatismOverlays } from "@assayline/api-server/domains/conservatism/handlers";
import type { ConservatismDDDDependencies } from "@assayline/api-server/domains/conservatism";
import { createConservatismOverlay as createConservatismOverlayEntity } from "@assayline/factories/conservatism";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@assayline/tests/shared";
import { CONSERVATISM_USE_CASES } from "../__factories__/use-cases.js";

describe("ListConservatismOverlays", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: ConservatismDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<ConservatismDDDDependencies>(
      CONSERVATISM_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with list of entities", async () => {
    const entities = [
      createConservatismOverlayEntity({ orgId }),
      createConservatismOverlayEntity({ orgId }),
    ];
    const useCaseResult = {
      data: { items: entities, nextCursor: undefined, prevCursor: undefined },
      meta: { correlationId: "test", timestamp: new Date().toISOString(), pagination: {} }
    };
    
    vi.spyOn(mockDeps.executeListConservatismOverlays, "execute").mockResolvedValue(useCaseResult);

    await listConservatismOverlays(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeListConservatismOverlays.execute).toHaveBeenCalledWith(expect.anything());
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
