/**
 * GetCapacityForecast handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: validation
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getCapacityForecast } from "@assayline/api-server/domains/validation/handlers";
import type { ValidationDDDDependencies } from "@assayline/api-server/domains/validation";
import { createCapacityForecast as createCapacityForecastEntity } from "@assayline/factories/validation";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@assayline/tests/shared";
import { VALIDATION_USE_CASES } from "../__factories__/use-cases.js";

describe("GetCapacityForecast", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: ValidationDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<ValidationDDDDependencies>(
      VALIDATION_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity", async () => {
    const entity = createCapacityForecastEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetCapacityForecast, "execute").mockResolvedValue(useCaseResult);

    await getCapacityForecast(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetCapacityForecast.execute).toHaveBeenCalledWith(expect.anything());
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
