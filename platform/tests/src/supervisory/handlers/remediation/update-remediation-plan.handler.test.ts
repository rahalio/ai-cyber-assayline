/**
 * UpdateRemediationPlan handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: supervisory
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { updateRemediationPlan } from "@assayline/api-server/domains/supervisory/handlers";
import type { SupervisoryDDDDependencies } from "@assayline/api-server/domains/supervisory";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@assayline/tests/shared";
import { SUPERVISORY_USE_CASES } from "../__factories__/use-cases.js";

describe("UpdateRemediationPlan", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: SupervisoryDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<SupervisoryDDDDependencies>(
      SUPERVISORY_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with updated entity", async () => {
    const entity = { id: 'mock-id', orgId } as any;
    const input = {} as any;
    const updatedEntity = { ...entity, ...input };
    const useCaseResult = { data: updatedEntity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    mockRequest.params = { addOnId: entity.id };
    mockRequest.body = input;
    vi.spyOn(mockDeps.executeUpdateRemediationPlan, "execute").mockResolvedValue(useCaseResult);

    await updateRemediationPlan(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeUpdateRemediationPlan.execute).toHaveBeenCalledWith(expect.objectContaining({ addOnId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
