/**
 * ListLumpSumBuffers handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: quantification
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { listLumpSumBuffers } from "@assayline/api-server/domains/quantification/handlers";
import type { QuantificationDDDDependencies } from "@assayline/api-server/domains/quantification";
import { createLumpSumBuffer as createLumpSumBufferEntity } from "@assayline/factories/quantification";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@assayline/tests/shared";
import { QUANTIFICATION_USE_CASES } from "../__factories__/use-cases.js";

describe("ListLumpSumBuffers", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: QuantificationDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<QuantificationDDDDependencies>(
      QUANTIFICATION_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with list of entities", async () => {
    const entities = [
      createLumpSumBufferEntity({ orgId }),
      createLumpSumBufferEntity({ orgId }),
    ];
    const useCaseResult = {
      data: { items: entities, nextCursor: undefined, prevCursor: undefined },
      meta: { correlationId: "test", timestamp: new Date().toISOString(), pagination: {} }
    };
    
    vi.spyOn(mockDeps.executeListLumpSumBuffers, "execute").mockResolvedValue(useCaseResult);

    await listLumpSumBuffers(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeListLumpSumBuffers.execute).toHaveBeenCalledWith(expect.anything());
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
