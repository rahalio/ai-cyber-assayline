/**
 * Submission Entity Factory
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: submissions
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { faker } from "@faker-js/faker";
import type { SubmissionEntity } from "@assayline/core/submissions/models/index.js";
import { submissionsSchemas } from "@assayline/core/submissions";
import { ulid } from "ulid";

// Domain prefix from OpenAPI spec info.x-domain
const DOMAIN_PREFIX = "sub";

export function createSubmission(overrides: Partial<SubmissionEntity> = {}) {
  const raw = {
    id: overrides.id ?? `${DOMAIN_PREFIX}_${ulid().toLowerCase()}`,
    createdAt: overrides.createdAt ?? new Date().toISOString(),
    updatedAt: overrides.updatedAt ?? new Date().toISOString(),
    modelId: overrides.modelId ?? faker.string.uuid(),
    status: overrides.status ?? "received",
    submittingFunction: overrides.submittingFunction ?? faker.lorem.word(),
    ...overrides,
  };

  return submissionsSchemas.Submission.parse(raw);
}

export function submissionBatch(n = 3, overrides = {}) {
  return Array.from({ length: n }, () => createSubmission(overrides));
}
