/**
 * Validation DDD Dependencies - Composition root
 *
 * Generated by Dependencies Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: validation
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * Validation Domain Module - Composition root
 *
 * DDD: Composition root for validation domain. Exposes repos (ports) and use cases
 * grouped by aggregate. No orgId at build time — use execution context at call time.
 */

import {
  CapacityForecastRepositoryAdapter,
  EngagementRepositoryAdapter,
  SlotRepositoryAdapter,
  TaskRepositoryAdapter,
} from "@assayline/adapters/validation";
import { getIdGeneratorService } from "@assayline/adapters";
import type { AdapterDynamoDBClient } from "@assayline/adapters";
import { executionContextService } from "../../../lib/execution-context.service.js";
// Use-case constructors (concrete)
import {
  ExecuteAllocateValidationSlot,
  ExecuteCreateValidationEngagement,
  ExecuteGetCapacityForecast,
  ExecuteListValidationEngagements,
  ExecuteListValidationSlots,
  ExecuteListValidationTasks,
  ExecutePlaceValidationTask,
} from "@assayline/services/validation/usecases";
// Ports (interfaces)
import type {
  CapacityForecastRepository,
  EngagementRepository,
  SlotRepository,
  TaskRepository,
} from "@assayline/services/validation/ports";

export interface ValidationDomainModule {
  repos: {
    capacityForecasts: CapacityForecastRepository;
    engagements: EngagementRepository;
    slots: SlotRepository;
    tasks: TaskRepository;
  };
  useCases: {
    capacityForecasts: {
      get: ExecuteGetCapacityForecast;
    };
    engagements: {
      create: ExecuteCreateValidationEngagement;
      list: ExecuteListValidationEngagements;
    };
    slots: {
      get: ExecuteAllocateValidationSlot;
      list: ExecuteListValidationSlots;
    };
    tasks: {
      get: ExecutePlaceValidationTask;
      list: ExecuteListValidationTasks;
    };
  };
}

export function buildValidationDomainModule(
  dynamoClient: AdapterDynamoDBClient,
): ValidationDomainModule {
  // Adapters implementing ports (adapters encapsulate Ddb)
  const repos = (() => {
    const obj: {
      capacityForecasts?: CapacityForecastRepository;
      engagements?: EngagementRepository;
      slots?: SlotRepository;
      tasks?: TaskRepository;
    } = {};

    obj.capacityForecasts = new CapacityForecastRepositoryAdapter(dynamoClient);
    obj.engagements = new EngagementRepositoryAdapter(dynamoClient);
    obj.slots = new SlotRepositoryAdapter(dynamoClient);
    obj.tasks = new TaskRepositoryAdapter(dynamoClient);
    return {
      capacityForecasts: obj.capacityForecasts!,
      engagements: obj.engagements!,
      slots: obj.slots!,
      tasks: obj.tasks!,
    };
  })();

  const executionContext = executionContextService;
  const idGenerator = getIdGeneratorService();

  // Use cases depend only on ports (repos.*)
  const useCases = {
    capacityForecasts: {
      get: new ExecuteGetCapacityForecast(executionContext, idGenerator, repos.capacityForecasts),
    },
    engagements: {
      create: new ExecuteCreateValidationEngagement(executionContext, idGenerator, repos.engagements),
      list: new ExecuteListValidationEngagements(executionContext, idGenerator, repos.engagements),
    },
    slots: {
      get: new ExecuteAllocateValidationSlot(executionContext, idGenerator, repos.slots),
      list: new ExecuteListValidationSlots(executionContext, idGenerator, repos.slots),
    },
    tasks: {
      get: new ExecutePlaceValidationTask(executionContext, idGenerator, repos.tasks),
      list: new ExecuteListValidationTasks(executionContext, idGenerator, repos.tasks),
    },
  };
  return { repos, useCases: useCases as ValidationDomainModule["useCases"] };
}
